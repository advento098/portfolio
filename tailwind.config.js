module.exports = {
    theme: {
        extend: {
            width: {
                'custom-clamp': "clamp(3rem, 50vw, 5rem)"
            },
            boxShadow: {
                'hero-button': '0px 0px 40px 6px rgba(0,196,204,0.6)'
            }
        }
    },
    plugins: [],
}